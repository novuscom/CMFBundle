{% extends 'NovuscomCMFBundle:Default:admin.html.twig' %}

{% block body -%}
    <div class="box">
        <div class="box-header">
            <h4 class="box-title">Создание сайта</h4>
        </div>
        <div class="box-body">
            {{ form(form) }}
        </div>
        <div class="box-footer">
            <a class="btn btn-info" href="{{ path('cmf_admin_site_list') }}"><i class="glyphicon glyphicon-list"></i> Список сайтов</a>
        </div>
    </div>
{% endblock %}