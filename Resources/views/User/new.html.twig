{% extends 'NovuscomCMFBundle:Default:admin.html.twig' %}

{% block body -%}

    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Создание нового пользователя</h3>
        </div>
        <div class="box-body">
            {{ form(form) }}
        </div>
        <div class="box-footer">
            <a class="btn btn-info" href="{{ path('admin_user') }}">
                <i class="glyphicon glyphicon-list"></i> Список пользователей
            </a>
        </div>
    </div>
{% endblock %}
